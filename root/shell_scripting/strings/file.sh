#! /bin/bash
# python program to replace all occurences of 'a' with $ in a string

read -p m
str = m.replace('a','$')
echo str#! /bin/bash
# python program to replace all occurences of 'a' with $ in a string

read -p m
str = m.replace('a','$')
echo str
